(function(window) {
  'use strict';
  var app = window.app || {};
  app.newAjax = app.newAjax || {};
  app.newAjax.lightAjax = function() {
    // 1ms後に実行する
    /* Question */
  };
  window.app = app;
})(window);
