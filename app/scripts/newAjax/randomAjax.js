(function(window) {
  'use strict';
  var app = window.app || {};
  app.newAjax = app.newAjax || {};
  app.newAjax.randomAjax = function() {
    // 1 ~ 10mxのランダムな時間後に実行する
    /* Question */
  };
  window.app = app;
})(window);
