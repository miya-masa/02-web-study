(function(window, $) {
  'use strict';
  var app = window.app || {};
  app.newAjax = app.newAjax || {};
  app.newAjax.heavyAjax = function() {
    // 5ms後に実行する
    /* Question */
  };
  window.app = app;
})(window, jQuery);
