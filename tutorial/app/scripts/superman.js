(function(window) {
  'use strict';
  var app = window.app || {};
  /* Question */
  // app.Superman
  // app.Personを親とする
  // コンストラクタで親のコンストラクタをコール
  // プロトタイプはObject.createを利用する
  // 子供特有のメソッドは以下の通り
  // fly()
  // superHelloMessage()
  // 仕様についてはsupermanSpec.jsを参照すること
  window.app = app;
})(window);
