(function(window) {
  'use strict';
  var app = window.app || {};
  /* Question */
  // app.Person
  // nameとgreetingを持つクラス
  // helloMessageを関数として持つ
  // 詳しい使用はpersonSpec.jsを参照すること
  window.app = app;
})(window);
